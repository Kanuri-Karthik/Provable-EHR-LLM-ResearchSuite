// NOTE: For the AI features to work, this placeholder key needs to be replaced with a valid Gemini API key.
// It is recommended to use environment variables in a real production setup instead of hardcoding keys.
export const GEMINI_API_KEY = 'YOUR_API_KEY_HERE';
